import ContactForm from '@/components/ContactForm';
export const metadata = { title: 'Discuss a challenge', description: 'A private first brief — device-local until you deliberately complete it.' };
export default function Page() { return <ContactForm />; }
